// Here create whole the components referent to /containers. e.g 
// If into container exist a folder called /Home, so, in /components may have a folder called /Home
// and handle the components interns into the Home.jsx view.